# 开工单 · 2026-09-17 公司电脑（给 Ann 和施工猫照着念）

> 两边是两个网络环境，各管各的，别混。

## 家里那边（昨晚的状态，30 秒读完）

- 今晚在家修了什么、坏在哪、存档在哪，全写在这份单子里（推送后公司能拉到）。
- **家里电脑白天可以不管**：不关机最好（手机家里那份测试存档绑在 `https://192.168.0.103:5173`），但人不用碰它。
- 家里那份手机存档和公司那份**是两份独立数据**（地址不同=两间屋子），互不相干，都不丢。

## 公司这边：早上就一步（Ann 照着念给 AI 听）

> 「git pull 拉远端最新代码，然后打开 开工单-0917早上.md，照单子干」

剩下的它照单干。就这一句。

**公司网络注意（出问题就这么办）：**
- pull 卡住、报代理/网络错误 → 把报错原样丢给 AI 处理（公司机有代理和 VPN 的老规矩，AI 懂），你不用动手。
- 公司机 git 的老毛病（commit 后 refs 被吃）AI 有绕法，遇到异常先停下报你，别硬试。
- 公司测试照常用公司地址 `10.48.18.221:5188`，那份存档一直是公司的，昨晚家里的事不影响它。

## 昨晚结论（不用再怀疑的实锤）

- v8d 修了六条，**真机实测 @ 和私聊还是坏的**。
- 部署没问题（抓包验证过服务端发的是新代码）→ **是新代码里有真 bug**，不是部署、不是缓存。
- 还原点 `9140bda2`（v8b+v8c），出问题随时退回。

## 真凶排查清单（施工猫按顺序查）

三条症状（@ 不回、没私聊、@ 评论不同步进聊天）很可能是**一条链断了全断**：

1. **发帖 @ 的登记链**：`handleCreatePost` 存帖子带 `mentions`（SocialApp.tsx 约 L1202）——存完**有没有逐个调 syncPostToChar**（@ = 同步 = 进私聊卡 + 进追踪名单）。没调 → 名单空 → 私聊节永不出现。
2. **搅动/首评强制名单漏了发帖 @**：若只取 `lastUserCommentRef`（评论 @，约 L1575-1596），**post.mentions 没并进去** → 发帖 @ 的角色不被强制 → 「@ 不回」。施工单写了「@ 必须回」，验收必须对这条。
3. **首评 privateChat 输出格式解析是否坏**：解析出错 = 整批首评落不了地 = 「没人回」。
4. 工具：v8b 埋的 `[Spark]` 日志还在，浏览器 console 看。

**警示**：昨晚抓过现行——施工单定稿没被执行（「返回空数组」句从未删过）。验收逐字对提示词，别信「改完了」三个字。

## 铁律照旧

- 默认计划模式；commit / push 每一步单独问 Ann。
- 三验收：tsc 47 基线；vitest **只跑 Spark 相关 4 个测试文件**（全量禁跑）；build 最后单独跑。
- 汇报大白话，4 分钟没动静必须报进度。
