# HANDOFF-spark-v8d.md — 修复施工单（终稿 · Ann 全部拍板，2026-09-17 凌晨）

> 本单整份替代同名旧稿与全部讨论稿，只做本单列出的事。
> 提示词改动只许照抄「六、提示词定稿」的逐字内容；Ann 逐字定稿过的句子一个字不许改。
> 铁律：只动点名文件与位置；改完跑三验收（tsc 47 基线 / vitest / build），停在 commit 前报数字。

## 一、要修的问题（Ann 实测报的 5 条）

1. Spark 管理面板点头像/账号入口时，界面闪一帧主页再弹回，卡顿感明显（不影响数据）。
2. 编辑帖子：取消勾选「不给谁看」后 @ 某角色，保存后没同步进 Ta 私聊（帖子卡没发过去）。
3. 同上操作后，搅动时该角色不出现评论。
4. 「不给谁看」和「同步」的口径混了：不给谁看只管人设/记忆/历史聊天记录，拦不住同步。
5. 恶性：同步/@ 之后怎么搅动都出不了私聊；@ 的角色也不回评论。

## 二、根因（排查结论，全部实锤，行号为 2026-09-17 凌晨现场）

- **随机抽 2**：`selectSparkParticipants`（utils/socialGeneration.ts L70-88）从乱序候选里只补到 2 个、结尾截 4。发给 AI 的角色名单因此只剩 2-4 人，与 Ann 的设计（全部角色发给你，谁评论你定）不符。
- **私聊提示词开关卡错条件**：SocialApp.tsx L1493 `finalChars.some(c => syncedIds.includes(c.id))` ——被同步的角色若没被随机抽进本轮名单，这个条件就查不到任何一个同步角色 → 整段私聊提示词消失 → 私聊全灭。Ann 定稿口径：**只要同步过人（追踪名单里至少一个人），私聊提示词就要出现**。
- **编辑保存不处理 @**：`handleCreatePost` 编辑分支（SocialApp.tsx L1062-1086）只更新标题/正文/图/tag/圈子/不给谁看，`post.mentions` 与同步链完全没人管；`startEditPost` L1051 还把 @ 选择清空。发帖分支（L1105、L1121-1124）是好的：`mentions` 入库 + 逐个 `syncPostToChar(post, cid, true)`（帖子卡进私聊 + trackSparkPost 登记）。
- **候选池缺同步对象**：两处候选池（首评 L801-803、搅动 L1455-1457）现在的做法是「全部角色里，去掉勾了不给谁看的」。这样同步/@ 过的角色若恰好在「不给谁看」里，会被整个丢掉——Ann 拍板 B 案：同步这个动作本身 = 把记忆和上下文发给 Ta，**同步/@ 过的角色不受「不给谁看」拦截**。
- **首评没有私聊**：首评 prompt（L826-875）与解析（L883-918）都没有 privateChat 字段。Ann 定稿：同步过人，首评也要有私聊能力。
- **搅动 L1520**「也可能这段时间没人说话——一条新动静都没有（返回空数组）就空着。」：Ann 的最终定稿当时漏执行（e7bdee3f 进码后 git 历史零次删除），本单补删。

## 三、设计总规则（Ann 定稿，实现必须逐条对上）

用户发的帖子：
1. 发给 AI 的角色名单，按这个顺序三步构造：
   第一步：从「全部角色」开始。
   第二步：去掉用户勾了「不给谁看」的角色。
   第三步：把追踪名单里的角色（被同步过、或被 @ 过的）一个一个加回来。
   两条防错规则，必须遵守：
   a. 第三步加回来的人，就算他在第二步被去掉了（勾了「不给谁看」），也要留在名单里，他的人设和记忆照常装进请求。
   b. 去重：同一个角色在名单里只出现一次。他的人设和记忆整个请求里只装一份——绝不因为「既在全部角色里、又在追踪名单里」就装两份。
2. 选了圈子 → 提示词多一段该圈子的世界观原文，只有世界观，不带角色词条（现状已是，别动坏）。
3. @（发帖里或评论里出现就算）→ 该角色这一轮评论必须出现。@ 同时做两件事：把这个角色记进追踪名单、把帖子卡发进他的私聊——和点同步按钮效果一样。
4. 同步按钮做两件事：把这个角色记进追踪名单、把帖子卡发进他的私聊。他回不回评论，交给 AI 判断。
5. 只要同步/@ 过至少一个角色（追踪名单里至少有一个人）→ 私聊提示词出现（首评、搅动都出现）；用不用 AI 看上下文和帖子决定。
6. 谁来评论由 AI 写（概率写法约束，见提示词定稿），不再随机抽。

圈子帖（角色发 / 圈子刷新生成的）：
7. 圈子帖（角色发 / 圈子刷新生成的）名单，按这个顺序构造：从「该圈子的成员」开始（圈子帖没有「不给谁看」这个东西），再把追踪名单里的角色加回来（去重，同上）。楼主一定在名单里。
8. 刷新 = 生成新帖子（现状概率写法，一个字不动），不涉评论/同步/私聊。

保持不动（不许碰）：
9. 出口硬闸（L1580、L1589）：privateChat 只有追踪名单里的角色能落库；全局私聊开关照旧先生效。
10. 搅动私聊节正文、搅动节奏句其余部分、路人节、禁令节、同步卡文案、刷新提示词、`buildSparkGenerationContext` 的记忆/私聊片段格式：一字不动。
11. 删帖不清追踪名单（Ann 拍板不做）。

## 四、任务清单（SocialApp.tsx 除注明外）

### 任务 1 拆「随机抽 2」（utils/socialGeneration.ts）
`selectSparkParticipants`（L70-88）：去掉 `if (selected.length >= 2) break;` 和结尾的 `slice(0, 4)`——楼主与楼中楼作者优先收进的逻辑保留，其后**全部候选按序并入**，不再截断。函数签名、两个调用处不动。

### 任务 2 候选池补同步对象（B 案，两处同款）
首评 L800-803 与搅动 L1454-1457，把候选池的构造改成三步：
1. 起点：用户帖从「全部角色」开始；圈子帖从「该圈子的成员」开始。
2. 过滤：用户帖去掉勾了「不给谁看」的角色（圈子帖没有这一步）。
3. 补回：读取追踪名单 `loadTrackedSparkPosts()[post.id]?.charIds`，名单里每个角色如果不在候选池里就加进来；**已经在候选池里的，跳过不加**。
   ——「跳过」就是去重：一个角色只进一次候选池，后面人设和记忆只装一份，绝不允许重复装两份。
旧帖子没有追踪记录时，第 3 步等于没做，行为跟现在完全一样。

### 任务 3 首评提示词改造
- 3a. L836 `混合使用 **选定角色** 和 **随机路人**，自由参与。` 整行替换为「### 角色的出场节奏」整段（见六-A）。
- 3b. L841（`选定角色如果评论，要根据当前状态和关系进度评论。`）之后、「### 路人」节之前，新增条件节「## 用户点名的角色」（帖子上有 @ 时才渲染；见六-B）。
- 3c. 私聊节：`syncedIds.length > 0` 时（`syncedIds = loadTrackedSparkPosts()[post.id]?.charIds || []`），在「### 路人」节之前插入私聊节 = 搅动 L1493-1499 正文**逐字照搬**（见六-D）。
- 3d. 输出格式（L872-875）的 JSON 说明加 privateChat 字段（见六-C）。
- 3e. 解析端（L883-918）：给每条评论加 privateChat 处理，完全照抄搅动的落库写法（L1573-1589 区域）+ 同款硬闸（`syncedIds.includes(matchedChar.id)` 且 `!loadPrivateChatOff()[matchedChar.id]`）。

### 任务 4 搅动提示词改造
- 4a. L1520 整行删除。
- 4b. L1523 出场句后新增一行社交面具句（见六-E）。
- 4c. L1540-1541「### 用户点名的角色」节替换为「## 用户点名的角色」+ 内容扩充发帖 @ + 强调句式（见六-F）。
- 4d. L1459-1465 forcedChars（评论 @ 强制并入）逻辑保留不动。
- 4e. L1493 私聊节条件 `finalChars.some(c => syncedIds.includes(c.id))` → `syncedIds.length > 0`（追踪名单里至少有一个人，私聊节就出现）。注释（L1489-1491）同步改写为本单三-5 口径。私聊节正文一字不动。

### 任务 5 编辑保存补 @ 同步链
- 5a. `startEditPost` L1051 `setNewPostMentions([])` → 回填 `setNewPostMentions([...(post.mentions || [])])`（编辑面板能看到已有 @，选择器可增）。
- 5b. 编辑保存分支（L1062-1086）：`updatePostInFeed` 里加 `mentions: [...newPostMentions]`；落定后对**新增**的 @（在 newPostMentions、不在原 post.mentions）逐个 `await syncPostToChar(最新 post 对象, cid, true)`——写法照抄发帖分支 L1121-1124。原有 @ 不重复同步、不撤销。
- 5c. 其余保存行为一行不动。

### 任务 6 修闪屏 —— **已停办（2026-09-17 凌晨 02:17 Ann 拍板止损），改动全部撤回原样**

结论：淡入动画假设已被真机证伪（去掉淡入后仍闪且更明显）。逻辑层三条线此前已查干净，剩余方向（手机浏览器绘制层）深夜无法定位。**此条挂账「未修」，不阻塞前五个任务的验收**，白天再另议。

flash 现在做三件事：
1. `git restore components/os/Modal.tsx` —— 该文件的全部改动都是任务 6 的，整体还原安全。
2. `apps/SocialApp.tsx` 手工撤三处（只许动这三处，别的一个字不碰）：约 L2281-2283 的任务 6 注释与「身份管理」Modal 上的 `noOverlayFade`、约 L2466-2472 头像面板注释与 `noOverlayFade`。撤完与存档点 `9140bda2` 的行为完全一致。
3. WORKLOG 记一笔「任务 6 挂账未修，改动已撤回」。

撤完跑第五节低负荷验收，停在 commit 前报数字。**不许再试任何新的修法。**

## 五、完工验收（全过才算完；低负荷模式——2026-09-17 凌晨机器被并发全量体检压垮过， vitest 不许再跑全量）

1. 三验收：
   - tsc 全量跑（轻，随便跑）：47 基线、改动文件 0 新增；
   - vitest **只跑点名 4 个文件**：`pnpm.cmd vitest run utils/sparkCircles.test.ts utils/socialFeedMerge.test.ts utils/socialAppBlobRefs.test.ts utils/socialGeneration.test.ts`——全部通过即算过；若个别项报「超时/另XX正在进行」，单独重跑该文件一次，全绿即按负荷假警报处理，不许改生产代码；
   - `pnpm build` 最重，放最后单独跑，跑之前确认没有别的重活占内存；worker.bundle.js 无关不重生。
2. Ann 场景复测清单：
   - 同步 B 后任意次搅动 → 私聊提示词出现，B 的 privateChat 能落库；
   - 发帖或评论 @ A → A 这一轮评论必须出现；
   - 编辑：取消勾选「不给谁看」+ @ A + 保存 → A 私聊收到帖子卡；
   - 上一步之后搅动 → A 出现在评论区；
   - 勾了「不给谁看」且没同步的角色 → 人设和记忆不发；
   - 同步过但勾了「不给谁看」的角色 → 人设和记忆照常装进请求（只装一份）、可评论、可私聊；
   - 首评：同步过 → 私聊提示词出现；没同步 → 整节不出现；
   - 圈子帖：楼主在名单里，圈子成员全量进请求。
3. 停在 commit 前，报数字等 Ann。

## 六、提示词定稿（逐字，照抄）

### A. 首评 L836 替换段（Ann 定稿；「路人们」为 Ann 授权错别字修正）

```
### 角色的出场节奏
跟帖子内容越相关的角色，越可能开口——Ta 的人格、记忆、最近在意的事跟这条帖子越贴，越可能评论，像真人刷到自己在意的话题忍不住说话。相反，对该话题讨厌，避而不谈，没有兴趣或者普普通通的，跟帖子没什么关系的角色，更多是划过去没说话，真人不会逢帖必评。尤其是对网友的倾诉欲是搁着一层社交面具的。（社交面具：指的是角色想要在圈子里保持的形象，并非100%的现实原生性格，而是互联网的侧面的性格，与角色的人设，性格，心态和需求有关。）
一次的新评论保持自然的量（不超过 6 条），不要让角色和路人们一哄而上全部开口，评论应该缓缓展开讨论。
```

### B. 首评新增节（帖子上有 @ 时才渲染；写法按 Ann 要求用强调语法）

```
## 用户点名的角色
用户发布这条笔记时 @ 到的角色：**这条笔记 Ta 收到了。Ta 怎么回应由人设决定，但这一轮必须出现，不会像没看见一样。**
```

### C. 首评输出格式（替换 L872-875 的 JSON 说明）

```
### 输出格式 (JSON Array)
[
  { "author": "网名 (Handle) 或 路人昵称", "charId": "角色ID或null", "content": "评论内容...", "replyTo": "被回复的评论作者名，不回复填null", "privateChat": ["想说的第一条", "第二条", ...] 或省略 }
]
```

### D. 首评私聊节 = 搅动 L1493-1499 逐字照搬（含「### 私聊」标题）

```
### 私聊
角色有可以私聊用户的能力，可以用 "privateChat": ["内容1", "内容2"] 把想对用户一个人说的话直接发进
角色和用户的私聊——只有用户看得到，评论区不显示。
它承载的是只想让用户一个人听到的话：悄悄话、贴脸的话、想避开评论区认真聊几句的。
要不要私聊、什么时候私聊、说几条，跟在评论区发言一样——按角色当下的想法判断。
私聊的节奏跟着用户走：如果Ta 只是在帖子活动、有还没拿主意的事，不要急着替 Ta 做决定或把事情推进下去。
发私聊前先翻最近的私聊记录：你已经发过的内容，不要再重复发。
```

### E. 搅动 L1523 出场句后新增一行（Ann 定稿）

```
尤其是对网友的倾诉欲是搁着一层社交面具的。（社交面具：指的是角色想要在圈子里保持的形象，并非100%的现实原生性格，而是互联网的侧面的性格，与角色的人设，性格，心态和需求有关。）
```

### F. 搅动 L1540-1541 替换

```
## 用户点名的角色
用户发布这条笔记时 @ 到的角色、用户评论里直接 @ 到的角色、或用户在楼中楼里回复的那个角色：**Ta 看到了。Ta 怎么回应由人设决定，但这一轮必须出现，不会像没看见一样。**
```
