# HANDOFF-spark-v8b · Ann 下午复检增补单（v8 施工后 9 条 · 二改修订版）

> 定位：v8 施工单（HANDOFF-spark-v8.md）的增补，不是替代。v8 没做完的照旧做，本单是 Ann 下午复检新报的 9 条。
> 铁律不变：逐字定稿句一字不动、二改原则（原有功能只挪不丢）、三验收（tsc 47 基线 / vitest / build）、commit 前停下等 Ann 点头。
> 环境：工作区已有未提交的 v8 施工改动（SocialApp.tsx / SparkPostImage.tsx），在它之上继续改，别回退。
> 行号以当前工作区文件为准（v8 施工后），施工前先用锚点文字定位复核。

## 一、实锤 bug，直接修

**3. 圈子删除加防误删确认**
- 位置：`apps/SocialApp.tsx` `deleteCircle`（锚点：`const deleteCircle = (circleId: string)`）。
- 现状：圈子条目 × 按钮点击直接删，无确认——Ann 手滑删了整个世界。
- 修法：函数开头加 `if (!window.confirm(\`删除圈子「${name}」？帖子会保留在「全部」\`)) return;`（名字从 circles 按 id 查）。与删评论同款 confirm 风格，不动其他逻辑。

**6. 识图只识一次，结果缓存（Ann 拍板：要第二种）**
- 位置：`apps/SocialApp.tsx` `buildSparkFetchInit`（锚点：`const buildSparkFetchInit = async (`）。
- 现状：每次生成评论/搅动都从 DB 重新读图、整图附进请求——图帖每搅动一次烧一次识图 token。
- 修法：
  1. `SocialPost` 类型加可选字段 `imageCaption?: string`（types 文件同步）。
  2. 首次：发现有 `sparkimg:` 图且没有 `imageCaption` → 先用现有识图通道（visionApi 优先/全局模型）发一次独立请求「用一两句话客观描述这张图的内容」，描述存进帖子 `imageCaption`（走 `updatePostInFeed` 落库）。
  3. 之后：`imageCaption` 存在 → 不再附图，在 prompt 文本后附 `[图片内容：${imageCaption}]`。原附注句「（附注：这条帖子带了用户上传的图片，已附在消息里。评论可以自然地聊到图里的内容，也可以不提。）」原样保留，跟描述文本一起附。
  4. 用户编辑帖子换图/删图（`handleCreatePost` 编辑分支）→ 清空 `imageCaption`，下次重新识。
- 注意：识图描述请求失败不要阻塞评论生成——降级回现在的老路（直接附图）。无图帖路径完全不受影响。

**9. 用户发帖点赞随机——用推荐流同款范围**
- 位置：`apps/SocialApp.tsx` `handleCreatePost` 新建分支（锚点：`likes: 0,`，附近有 `isCollected: false`）。
- 修法：改成 `likes: Math.floor(Math.random() * 100)`——与刷新推荐流生成帖完全同一个公式同一个范围（推荐流那边就是这句）。二次编辑分支不动（注释已声明不改点赞）。
- 收藏数：**不用做**。帖子详情页的收藏数字已有（v9 二轮做的「收藏位装饰数字」，按帖子 id 稳定伪随机 0-99、不存库），Ann 没记错。

## 二、prompt / 文案改句（原文对照全部列出，Ann 逐字核对定稿后才落码）

**4. 删角色回复后执着重发 → 通知文案加一句**
- 位置：`apps/SocialApp.tsx` `handleDeleteComment` 的系统通知。
- 原文（现状，落进角色聊天的 system 消息）：
  `[系统: 用户行使管理权限，删除了《${post.title}》下 ${comment.authorName} 的一条回复（"${snippet}…"）。评论区里不会再有它。]`
- 改为（句末追加一句 + 开头「用户」换名字，两处都改）：
  `[系统: ${userProfile.name} 行使管理权限，删除了《${post.title}》下 ${comment.authorName} 的一条回复（"${snippet}…"）。评论区里不会再有它。不要执着地把被删掉的内容再写一遍。]`
- 定稿说明：追加句是 Ann 2026-09-16 逐字定稿，一字不许动；「用户」→`${userProfile.name}` 只在这一句开头做（Ann 明确：只有这一处）。

**5. 「用户」→ 名字变量（Ann 定稿：全库只有一处）**
- **只改一处**：第 4 条系统通知句开头 `[系统: 用户行使管理权限` → `[系统: ${userProfile.name} 行使管理权限`。
- 变量：`userProfile.name`（该作用域已在用，无需新引入）。
- **其他任何地方的「用户」一个都不许改**——私聊说明段、搅动 prompt 各段、toast、按钮文案，全部保持原样。Ann 原话：只有那一处前端可见的通知而已，其他全部不能改。

**7. 私聊不许抢跑（Ann 逐字定稿）**
- 位置：私聊说明段内，插在「要不要私聊、什么时候私聊、说几条，跟在评论区发言一样——按角色当下的想法判断。」这一行**之后**、「### 新刷进来的陌生网友」标题**之前**，独立成段：
  ```
  私聊的节奏跟着用户走：如果Ta 只是在帖子活动、有还没拿主意的事，不要急着替 Ta 做决定或把事情推进下去。
  ```
- 定稿说明：Ann 2026-09-16 逐字定稿（含「如果Ta」的空格位置），一字不许动，**不改变量**——句中「用户」保持原样。

**8. 私聊不重复发同内容（Ann 逐字定稿）**
- 位置：同上，紧接第 7 条新增段之后，追加一行：
  ```
  发私聊前先翻最近的私聊记录：你已经发过的内容，不要再重复发。
  ```
- 定稿说明：Ann 逐字定稿，一字不许动。此前拟的「私聊是稀缺动作…」整段**不要**——Ann 只留这一句。

## 三、修法已定，等 Ann 最终点头

**2. 评论删光 → 卡片按「同一张卡删光为准」删（Ann 纠正口径）**
- Ann 口径：帖子和评论后续更新不是一张卡——判定删卡不看帖子全局空不空，**每张卡按自己快照里的评论算**。
- 位置：`apps/SocialApp.tsx` `syncPostSnapshotToChats`（锚点：`const emptied` 那段，现为「正文空+评论删光」全局一刀切——作废）。
- 改法：
  1. **动态更新卡**（`syncKind: 'update'`，`metadata.newComments` 带新评论名单）：`newComments` 里的评论 id 全部不在帖子当前评论里了 → 这张卡 `DB.deleteMessage` 删掉；还有存活的 → 走现有 `updateMessageMetadata` 刷新（newComments 同步过滤，现在已有这逻辑）。
  2. **首卡/角色侧同步卡**（分享卡、`syncKind` 非-update 的同步卡）：帖子正文空 **且** 评论删光 → 删；否则保留、评论刷成最新（现有 update 逻辑）。
  3. 全局 `emptied` 一刀切判定删除。
- 效果：Ann 下午的场景（评论删光、帖子还在）→ 那些新动态通知卡一张张消失，帖子本身的卡留着但评论区空。

**1. 评论修改后聊天里还是旧评论（链路三环都查过正常，带日志实测定位）**
- 已排除三个嫌疑（都翻过代码实锤正常）：改评论→内存帖子更新（feedRef 同步赋值，无竞态）→写聊天卡（`DB.updateMessageMetadata` 真读真写事务）→覆盖名单（追踪角色∪发过言角色，发过言的跑不掉）。
- 所以问题藏在某一环的具体场景里，施工猫先复现别瞎改：
  1. 在 `syncPostSnapshotToChats` 的 for 循环加 `console.debug('[Spark][快照同步]', postId, charId, cards.length)`（仿现有 `[Spark][追踪通知]` 日志风格）。
  2. 复现：改一条已被同步过的评论 → **重进**该角色聊天看卡。
  3. 三种结果对三种修法：
     - 重进后卡是新的 → 根因 = 打开中的聊天窗口不刷新（DB 是对的，UI 没重读）→ 修 Chat 侧消息列表刷新；
     - 日志没打出 → 同步名单漏了该角色 → 修名单口径；
     - 日志打出了、重进还是旧的 → `updateMessageMetadata` 写入没生效 → 查该消息 id 的事务（card.id 类型、消息是否存在）。
  4. 拿到哪种实锤修哪种，日志验证后可留 dev 调试用（参考 docs/dev-debug.md 的开关规矩）。

## 验收

- 老三样：tsc（47 错误基线）/ vitest / build。
- prompt 句（4/5/7/8）**全部已 Ann 逐字定稿**（2026-09-16），落码时一字不许动、位置不许挪；施工后自查时把这四句 diff 出来贴给 Ann 复核。
- 自查：识图缓存改动不破坏无图帖路径；likes 随机不影响二次编辑；「用户」替换没碰 UI 文案；删卡逻辑改动不影响正常评论刷新路径（有活评论的卡必须还在）。
- 完工停在 commit 前，报 Ann。
