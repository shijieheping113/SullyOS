# SullyOS 公司工位 · 工作日志

> **这份文件是给家里猫儿看的。**
> 它只存在于 `company/worklog` 这一个分支上，永远不会被合进任何功能代码分支。
> 规矩：公司这边每干完一件事，都要更新这份日志并推送。
> 用户（老板）没有任何代码基础，所有解释请保持白话。

---

## 📦 公司已推送分支清单（家里要合的就是这些）

| 分支名 | 内容 | 状态 |
|--------|------|------|
| （暂无功能分支） | —— | —— |

> 每推送一个新旁支，公司猫会更新这张表。**表里没有的分支就当不存在，不用管。**
> 公司猫承诺：**永远不碰** fork 上的 `master`，永远只推新分支。

---

## 🗓️ 2026-09-14（第一天）：把工位搭好了

今天没有改任何一行功能代码，只是搭环境。做了这些事：

1. **搞清楚了账号**：老板的 GitHub 账号是 `shijieheping113`。
2. **装了 git**：公司电脑原本没有 git，装的是 Git for Windows 2.55.0（git-lfs 也带上了）。
3. **下载了代码**：从作者仓库 `qegj567-cloud/SullyOS` 拉了原版最新版（master 分支，最新提交 `a5de4617`，2026-09-13 的）。
   - ⚠️ 注意：这是**作者原版**，不包含家里那份二改（语音、角色来电、拉黑等）。这是老板定的规矩，公司只基于作者原版开新旁支。
4. **配好了两个"远端地址"**：
   - `origin` = 老板自己的仓库 `https://github.com/shijieheping113/SullyOS.git`（这个是**私有 fork**，公司改的东西往这里推）
   - `upstream` = 作者仓库 `qegj567-cloud/SullyOS`（只从这里拉新版本，**永远不往作者那里推**）
5. **验证了推送权限**：干跑测试通过，公司的凭据可以推到老板的仓库。

**截至目前，老板 GitHub 仓库上没有任何代码变动**：master 还是 fork 时的样子，唯一推上去的就是这份日志所在的 `company/worklog` 分支。

---

## 🏠 家里电脑状态备忘（老板口述，公司猫记录）

- 家里那份是**完整二改**：有语音、角色来电、拉黑等功能，是主战场。
- 家里的改动**只 commit 了（本地存档），没有 push（没上传到任何云端）**，也没有连接远端仓库。
- 家里的版本**落后作者新版**，还没跟上。
- 老板的目标顺序：**回家后，家里先合上作者新版 → 再合公司做的 → 都确认没问题才动家里主分支**。
- 老板家里主分支叫 `my-custom`（如果实际名字不一样，以实际为准）。

---

## 🔒 先给老板吃的定心丸（白话解释「会不会覆盖家里」）

**不会的，原因很简单：**

1. **公司改的东西，推到的是 GitHub 上一个"新开的分支"** —— 就像往图书馆的书架上**加了一本新书**，别的书一本都不会动。公司的分支和家里的代码，在云端是**两个完全独立的房间**。
2. **家里那份代码在你家电脑的硬盘上**，公司电脑隔着网络根本摸不到它。想覆盖也够不着。
3. **"合并"只会在一种情况下发生：回家后，你在家里电脑上主动让猫儿去做。** 公司这边不做、也做不了任何"合并到家"的动作。
4. 就算回家合并时出岔子，git 每一步都有存档点，**可以退回合并之前的样子**。而且合并前还会先复制整个文件夹做备份（下面的计划里写了），等于上了双保险。
5. git 的合并规则是**保留两边的改动**，不是谁覆盖谁。只有两边改了同一个地方才会"起冲突"，起冲突时 git 会停下来等人挑，**不会自己悄悄删掉任何一边的功能**。

---

## 📋 回家合并计划（给家里猫儿的操作手册）

> 家里猫儿请**严格按顺序**做，每步做完检查再走下一步。老板没有代码基础，出问题请用白话跟老板确认，不要自作主张。
> 以下命令都在**家里那个二改仓库的文件夹**里执行（就是有语音/来电/拉黑功能的那份）。

### 第 0 步：备份（最重要，跳过这步不许继续）

把家里的整个仓库文件夹**复制粘贴一份**，改名成 `SullyOS-backup-2026-09-14` 之类的放在旁边。
这样不管后面发生什么，最坏情况就是删掉重拷，功能绝不可能丢。

### 第 1 步：给家里的二改上一道"云端保险"

家里的改动现在只存在本地硬盘，硬盘一坏就全没了。先把它推到老板自己的 GitHub 仓库里存着（开一个 `home/my-custom` 分支放，和公司分支互不干扰）：

```bash
git remote add origin https://github.com/shijieheping113/SullyOS.git
git push -u origin my-custom:home/my-custom
```

（如果家里主分支不叫 `my-custom`，把命令里的名字换成实际的。）

### 第 2 步：把两边的"新货"都拉下来（只下载，不合并）

```bash
git remote add upstream https://github.com/qegj567-cloud/SullyOS.git
git fetch upstream
git fetch origin
```

到这一步为止，家里的代码**一个字都还没变**，只是把货拉到了门口。

### 第 3 步：开一个"试验分支"，在试验田里干活

**不要直接在 `my-custom` 上合并！** 先复制一个试验分支出来：

```bash
git checkout -b try/merge-company my-custom
```

### 第 4 步：先合作者新版

```bash
git merge upstream/master
```

- 有冲突就解决冲突（家里二改的地方和作者改动撞上了，需要挑）。
- 合完之后 `pnpm install` 装新依赖，然后 `pnpm dev` 跑起来，
  **逐项检查：语音、角色来电、拉黑，还有家里其他功能，全都在不在、正不正常。**
- 有问题就停下，用备份分支对照排查，别硬往下走。

### 第 5 步：再合公司的分支

看本日志最上面那张「公司已推送分支清单」，逐个合：

```bash
git merge origin/company/<分支名>
```

再跑一次、再检查一遍家里功能。

### 第 6 步：都确认没问题，才并回主分支

```bash
git checkout my-custom
git merge try/merge-company
```

试跑一两天没问题，试验分支可以留着也可以删（`git branch -d try/merge-company`）。

---

## 🚨 安全红线（家里猫儿同样要遵守）

1. **禁止整文件夹覆盖**家里的目录。
2. **禁止 `git push --force`**（强推会把云端记录砸掉）。
3. **禁止把任何东西推到作者仓库**（upstream 只拉不推）。
4. 合并只允许发生在**试验分支**上，确认后才能动 `my-custom`。
5. 老板说不清楚的事，**先问，别猜**。

---

*公司猫 Sully 值班记录中……🐾*
