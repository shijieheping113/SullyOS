# 小红书 · 评论输入区 — 设计参考规范

> **这份文档是给 AI 读的。**
> 目标：让你照着复刻这套界面的**外观**，并实现**三个交互**。
> 其余交互**不要参考**，因为源文件里很多没做完（详见第 6 节）。
>
> 源文件：`xhs-comment-input.html`（单文件，HTML + CSS + 原生 JS，无依赖）

---

## 0. 一句话说清要什么

复刻一个**手机端评论区界面**，包含：

| 部分 | 说明 |
|---|---|
| 顶部标题栏 | `共47条评论` + 排序图标 |
| 评论输入条 | 头像 + 灰胶囊「让大家听到你的声音」+ 语音图标 + 图片图标 |
| 评论列表 | 若干条评论，含昵称 / 正文 / 时间·地区·回复 / 点赞 / 表情 / 标签 |
| 楼中楼 | 主评论下的子回复组，**默认折叠**，可展开可收起 |
| 底部笔记互动栏 | 「说点什么…」+ 点赞 / 收藏 / 评论 |
| 输入弹层 | 点击输入条后从底部升起：多行输入框 + 工具行 + 表情行 + 键盘 |

**必须实现的交互只有三个**（第 5 节）：
1. 点击展开回复 / 收起回复（楼中楼）
2. 点击输入条 → 底部弹出输入框
3. 收起输入框

---

## 1. 设计令牌（Design Tokens）

直接照抄这套变量，不要另起一套配色。

```css
:root{
  /* 页面与容器 */
  --bg:          #FFFFFF;   /* 页面 / 面板底 */
  --bar-bg:      #FFFFFF;   /* 输入条底 */
  --field-bg:    #F2F2F2;   /* 胶囊 / 输入框底 */
  --field-bd:    #EDEDED;   /* 输入框描边 */
  --ph:          #B9B9B9;   /* 占位文字 */
  --tag-bg:      #F5F5F5;   /* 小标签底 */

  /* 文字层级 */
  --ink:         #1A1A1A;   /* 正文主色 */
  --ink-2:       #575757;
  --ink-3:       #9A9A9A;   /* 元信息 / 次要 */
  --ink-4:       #C4C4C4;
  --line:        #F0F0F0;   /* 分割线 */

  /* 品牌 */
  --red:         #FF2442;
  --send-off:    #FFC9D2;   /* 发送按钮 · 禁用态 */
  --send-on:     #FF2442;   /* 发送按钮 · 可用态 */

  /* 键盘 */
  --kb-bg:       #D1D5DB;   /* 键盘底 */
  --kb-key:      #FFFFFF;   /* 字母键 */
  --kb-fn:       #ADB3BC;   /* 功能键 */
  --kb-blue:     #3478F6;   /* 发送键 */
  --kb-ink:      #1A1A1A;
  --kw-bg:       #F2F3F5;   /* 键盘工具栏按钮底 */

  --sans: -apple-system,BlinkMacSystemFont,"PingFang SC","HarmonyOS Sans SC","Noto Sans CJK SC","Microsoft YaHei",sans-serif;
  --ease: cubic-bezier(.16,1,.3,1);   /* 全局缓动，所有过渡都用它 */
}
```

**视口**：移动端 `max-width:430px` / `height:100dvh`；桌面端呈现为 **393 × 852** 的手机壳（`border-radius:36px`）。

**图标**：全部是**内联 SVG 手绘线稿**（`stroke-width:1.6~1.9`、`stroke-linecap:round`、无填充），不是字体图标、不是位图。请沿用同样的线宽风格，不要混用实心图标。

---

## 2. 评论区外观

### 2.1 顶部标题栏

```css
height:52px; padding:0 16px; display:flex; align-items:center; gap:8px;
background:#FFFFFF;
/* 标题 */ font-size:17px; font-weight:600; letter-spacing:-.01em; color:#1A1A1A;
/* 排序 */ width:26px; height:26px; 图标 19px
```

⚠️ 标题栏是**吸顶固定**的，不跟随滚动。

### 2.2 评论输入条（视觉重点）

```css
.inputbar{
  display:flex; align-items:center; gap:8px;
  padding:9px 14px;
  background:#FFFFFF;
}
```

| 元素 | 尺寸 | 备注 |
|---|---|---|
| 头像 | 38 × 38，圆形 | 小红书默认**兔子线稿**头像：米白底 `#FFFDFA` + 深灰描边 `#7E7E7E`（1.7px） |
| 胶囊 | `flex:1`，高 **40**，圆角 **20**，左右内边距 **18** | 底色 `#F2F2F2`，**无边框**；按下变 `#ECECEC` |
| 胶囊文案 | 15.5px / `#B9B9B9` | 「让大家听到你的声音」，单行省略 |
| 两个图标按钮 | 36 × 36 容器，图标 **26px**，色 `#333333` | 语音 · 图片 |

**关键：输入条在滚动流里，不吸顶。** 往上滑会跟着内容滚出去，往下滑再滚回来（源文件里它被放在 `.panel__list` 内部，不是兄弟节点）。

### 2.3 单条评论

```css
.cmt{ padding:14px 16px 0; }
.cmt__row{ display:flex; gap:10px; }      /* 头像 + 主列 */
.cmt__av{ width:38px; height:38px; border-radius:50%; box-shadow:0 0 0 1px rgba(0,0,0,.04) inset; }
.cmt__name{ font-size:14.5px; line-height:1.3; color:#7A7A7A; }
.cmt__text{ margin-top:5px; font-size:16px; line-height:1.5; color:#1A1A1A; }
.cmt__meta{ margin-top:8px; display:flex; align-items:center; gap:10px;
            font-size:13.5px; color:#9A9A9A; }
.cmt__vote{ margin-left:auto; display:flex; align-items:center; gap:16px; }
.cmt__vote svg{ width:17px; height:17px; }        /* 心形 */
.cmt__emoji svg{ width:19px; height:19px; }       /* 笑脸 */
```

**两个容易做错的细节：**

1. 时间 / 地区 / 回复 **三者之间没有竖线**，只靠 10px 间隙分隔（很多复刻会加 `|`）。
2. 「首评」这类小标签：`font-size:10px; padding:3px 5px; border-radius:4px; color:#FF2442; box-shadow:0 0 0 1px currentColor inset;` —— 是**描边**样式，不是实底。

### 2.4 楼中楼（子回复）

```css
/* 整组回复容器 */
.cmt__replies{ display:flex; flex-direction:column; }
.cmt__replies[hidden]{ display:none; }                        /* ★ 必须有，见第 7 节 */
.cmt__replies:not([hidden]){ animation:replies-in .24s var(--ease) both; }
@keyframes replies-in{
  from{ opacity:0; transform:translateY(-4px); }
  to  { opacity:1; transform:none; }
}

/* 单条子回复：整块缩进到「主评论的文字列」= 头像38 + 间距10 */
.cmt__sub{ display:flex; gap:10px; margin:14px 0 0 48px; }
.cmt__sub .cmt__av{ width:26px; height:26px; }                /* 头像缩小 */
.cmt__sub .cmt__text{ margin-top:3px; }
.cmt__sub .cmt__meta{ margin-top:7px; }
.cmt__sub .cmt__vote{ gap:14px; }

/* 展开按钮 */
.cmt__more{ display:flex; align-items:center; gap:10px;
            margin:10px 0 0 48px; font-size:13.5px; color:#9A9A9A; }
.cmt__more::before{ content:""; width:22px; height:1px; background:#DCDCDC; flex:none; }
```

**缩进是唯一规则：48px。** 不区分「展开前 / 展开后」两套缩进——源文件早期版本试过两套，最后统一成一套。

**@ 提及的排版**（正文里提到某人）：

```html
<span class="mn"><b>美国点点</b><i>@</i></span>
```
```css
.mn b{ font-weight:600; }                 /* 昵称加粗 */
.mn i{ font-style:normal; font-size:.66em; vertical-align:.34em;
       margin-left:1px; color:#8A8A8A; }  /* 右上角小号 @，不是字面 @ 那个大小 */
```
效果是 `美国点点ᵃ` 那样——昵称加粗，后面跟一个上浮的小灰 `@`。

### 2.5 底部笔记互动栏

```css
.notebar{ height:56px; padding:0 14px 8px; display:flex; align-items:center; gap:14px;
          background:#FFFFFF; border-top:1px solid rgba(0,0,0,.03); }
.notebar__field{ flex:1; height:38px; border-radius:19px; padding:0 14px;
                 background:#F2F2F2; color:#B9B9B9; font-size:14px; }  /* 「说点什么…」+ 铅笔图标15px */
.notebar__act{ display:flex; align-items:center; gap:4px; font-size:12px; }
.notebar__act svg{ width:23px; height:23px; }
```

**这一条是固定吸底的**，不随列表滚动。

---

## 3. 输入弹层外观

### 3.1 遮罩 + 弹层容器

```css
.mask{ position:absolute; inset:0; z-index:20;
       background:rgba(0,0,0,.32);
       opacity:0; pointer-events:none;
       transition:opacity .3s var(--ease); }

.sheet{ position:absolute; left:0; right:0; bottom:0; z-index:30;
        background:#FFFFFF; border-radius:18px 18px 0 0;
        transform:translateY(102%);
        transition:transform .34s var(--ease);
        will-change:transform;
        display:flex; flex-direction:column; max-height:92%; }
```

⚠️ 弹层打开时，**原输入条留在原位、只是被遮罩压暗**——不要把它 `display:none` 掉。

### 3.2 多行输入框

```css
.sheet__editor{ margin:18px 16px 0; }
.editor{ width:100%; height:76px; padding:13px 14px;
         border:1px solid #EDEDED; border-radius:12px;
         font-size:15.5px; line-height:1.45; color:#1A1A1A;
         resize:none; outline:0;
         caret-color:#FF2442; }     /* 光标用品牌红 */
```

### 3.3 工具行

```css
.sheet__tools{ height:54px; padding:0 16px; display:flex; align-items:center; }
.tool{ width:52px; height:40px; display:grid; place-items:center; color:#232323; }
.tool svg{ width:26px; height:26px; }

.btn-send{ margin-left:auto; height:34px; min-width:62px; padding:0 18px;
           border-radius:17px; background:#FFC9D2; color:#fff;
           font-size:14px; font-weight:500; }
.btn-send.is-ready{ background:#FF2442; }
```

工具行 5 个图标：**语音 · 图片 · @ · 表情 · ＋**，右侧接「发送」胶囊。

### 3.4 表情行

```css
.sheet__stickers{ height:54px; padding:0 12px; gap:6px;
                  display:flex; align-items:center;
                  overflow-x:auto; scrollbar-width:none; }
.sticker{ width:46px; height:46px; flex:none; border-radius:10px;
          display:grid; place-items:center; font-size:30px; }
```

> 原图是小红书自有的 3D 卡通表情。此处用**系统 emoji 近似**——这只是占位，复刻时按需替换成真实贴图，外框尺寸按上表留好。

### 3.5 键盘工具栏

```css
.kbtools{ height:56px; gap:7px; padding:0 9px;
          display:flex; align-items:center;
          background:#FFFFFF; border-top:1px solid #F0F0F0; }

.kw{ width:38px; height:40px; border-radius:11px;
     background:#F2F3F5; color:#4A4A4A; display:grid; place-items:center; }
.kw svg{ width:22px; height:22px; }

.kw-voice{ flex:1; height:40px; border-radius:20px;
           background:#FFFFFF; box-shadow:0 0 0 1px #E4E4E4 inset;
           display:flex; align-items:center; justify-content:center; gap:6px;
           font-size:14px; }      /* 「点击说话」胶囊 */
```

排布：**5 个方按钮 ＋「点击说话」长胶囊 ＋ 收起箭头**。

### 3.6 九宫格键盘

```css
.keyboard{ background:#D1D5DB; padding:8px 5px 0; user-select:none; }
.kb__main{ display:grid;
           grid-template-columns:42px 1fr 1fr 1fr 62px;
           grid-template-rows:repeat(4,44px);
           gap:6px 5px; }
.kb__foot{ display:grid; grid-template-columns:56px 1fr 1.6fr 1fr 62px; gap:5px;
           padding:6px 0 10px; }

.k{ border-radius:7px; background:#FFFFFF; color:#1A1A1A;
    font-size:15px; box-shadow:0 1px 0 rgba(0,0,0,.16); position:relative; }
.k i{ position:absolute; top:3px; right:6px; font-size:10.5px; color:#8A8A8A; }  /* 长按候选字母 */

.k--fn{ background:#ADB3BC; font-size:17px; }        /* 功能键 */
.k--blue{ background:#3478F6; color:#fff; font-weight:500; }   /* 发送键 */
```

---

## 4. 状态与状态机

整个界面只有一个状态开关：给根节点 `.phone` 加 / 去 `.typing`。

```
默认态                              点击态（.phone.typing）
├─ 输入条可见、可交互      ──点击──▶   ├─ 输入条仍在原位，被遮罩压暗
├─ 遮罩 opacity:0, 不可点            ├─ 遮罩 opacity:1, 可点
└─ 弹层 translateY(102%)（屏外）      └─ 弹层 translateY(0)（升起）
```

```css
.phone.typing .mask { opacity:1; pointer-events:auto; }
.phone.typing .sheet{ transform:translateY(0); }
```

---

## 5. ★ 必须实现的三个交互

### 5.1 点击展开 / 收起回复（楼中楼）

**行为**：点「展开8条回复」→ 整组子回复出现；按钮文案变「收起回复」；再点 → **整组全部折掉，一条都不留**。

```html
<div class="cmt">
  <div class="cmt__row">…主评论…</div>

  <div class="cmt__replies" hidden>   <!-- 默认隐藏 -->
    <div class="cmt__sub">…</div>
    <div class="cmt__sub">…</div>
  </div>

  <button class="cmt__more" type="button" data-toggle-replies aria-expanded="false">
    展开8条回复
  </button>
</div>
```

```js
document.querySelectorAll('[data-toggle-replies]').forEach(function(btn){
  var host = btn.parentNode;                          // .cmt
  var box  = host.querySelector('.cmt__replies');
  if(!box) return;

  var expandText = btn.getAttribute('data-expand') || btn.textContent.trim();
  btn.setAttribute('data-expand', expandText);        // 存下原文案，收起后要还原

  btn.addEventListener('click', function(){
    var isOpen = !box.hasAttribute('hidden');
    if(isOpen){
      box.setAttribute('hidden','');
      btn.textContent = expandText;
      btn.setAttribute('aria-expanded','false');
    }else{
      box.removeAttribute('hidden');
      btn.textContent = '收起回复';
      btn.setAttribute('aria-expanded','true');
    }
  });
});
```

要点：
- 用 **`hidden` 属性**切换，不要用 `display` 直接改内联样式。
- 按钮原文案（「展开8条回复」）要从 DOM 里读出来存一份，否则收起后不知道还原成什么。
- 展开有 **0.24s** 淡入 + 上浮 4px 的入场动画；收起是瞬时的。
- 收起 = **全折**，不要留一条预览在外面。

### 5.2 点击输入条 → 弹出输入框

两个入口，行为完全一致：**顶部评论输入条** 和 **底部「说点什么…」**。

```js
function openSheet(){
  phone.classList.add('typing');                    // 遮罩淡入 + 弹层升起
  setTimeout(function(){
    editor.focus({ preventScroll:true });           // 延迟 180ms 再聚焦，等动画走完
  }, 180);
}
```

时序：`.typing` 加上后 → 遮罩 `opacity .3s` 淡入、弹层 `transform .34s` 从 `translateY(102%)` 升到 `0` → 180ms 后输入框聚焦、系统键盘跟着弹起。

> `preventScroll:true` 必要，否则聚焦会把底层列表滚跑。

### 5.3 收起输入框

三种收法，都要能收：

```js
function closeSheet(){
  phone.classList.remove('typing');
  editor.blur();
}

mask.addEventListener('click', closeSheet);        // 点遮罩
btnCollapse.addEventListener('click', closeSheet); // 点右下角收起箭头
document.addEventListener('keydown', function(e){
  if(e.key === 'Escape') closeSheet();             // Esc
});
```

---

## 6. ⚠️ 不要参考的部分（源文件里没做完）

**只抄外观和上面那三个交互。** 下面这些在源文件里要么是死按钮，要么是假实现，**不要照抄其逻辑**：

| 元素 | 实际状态 |
|---|---|
| **回复的点赞 / 心形** | **完全没有交互**。只是个 17px 图标 + 一个写死的数字，点了没反应，没有计数、没有状态切换 |
| **回复的表情按钮** | 死的，点了没反应 |
| 主评论的点赞 / 表情 | 同上，死的 |
| 排序按钮 | 死的 |
| 语音 / 图片 / @ / ＋ 工具按钮 | 全是死的，点了没反应 |
| 键盘工具栏的 5 个方按钮 | 死的 |
| **九宫格键盘** | **纯静态图文**。按键不产生输入，只有「发送」键绑了提交 |
| 表情插入 | 有个 `mousedown` + `preventDefault` 的小技巧防止失焦，逻辑本身可用但非重点 |
| 发送评论 | **本地演示**：直接 `createElement` 塞进 DOM，无网络请求、无校验、无失败态 |
| 头像 | 全是 **CSS 渐变占位块**，不是真实图片 |
| 底部笔记互动栏 | 仅作场景还原，无逻辑 |
| 表情行的 emoji | 用系统 emoji **近似**，非原始素材 |

> 如果你要接真实数据，这些位置都需要你自己补：点赞状态机、表情面板、真实键盘输入、提交请求与乐观更新、头像图片、排序切换。

---

## 7. 实现时最容易踩的坑

1. **`display:flex` 会盖掉 `[hidden]` 的 `display:none`。**
   给 `.cmt__replies` 写了 `display:flex` 之后，`hidden` 属性就**完全失效**，那组回复从头到尾都不会被隐藏，收起点了等于没点。
   **必须显式补一条 `.cmt__replies[hidden]{ display:none; }`。**

2. **输入条不是吸顶的。**
   它必须待在滚动列表**内部**，才能跟着内容滚出 / 滚回。标题栏才是固定的。

3. **弹层打开时输入条不要隐藏**，是被遮罩压暗的关系。

4. **同层级别叠 `display` 与 `hidden` 两套显隐机制**，二选一。

5. **所有过渡统一用 `cubic-bezier(.16,1,.3,1)`**，这是整套界面的手感来源。

6. **注释里带 `★` 的位置**是这套界面的复刻重点，别漏。

---

## 8. 结构骨架（可直接照此搭）

```
.phone                       ← 状态类 .typing 挂在这里
├── .panel
│   ├── .panel__head         ← 吸顶：共47条评论 + 排序
│   ├── .panel__list         ← 滚动容器
│   │   ├── .inputbar        ← ★ 输入条（在滚动流内 / 不吸顶）
│   │   ├── .cmt             ← 评论 1
│   │   │   ├── .cmt__row    ← 头像 + 主列（昵称/正文/meta/标签）
│   │   │   ├── .cmt__replies[hidden]   ← ★ 楼中楼，默认折叠
│   │   │   └── .cmt__more[data-toggle-replies]   ← ★ 展开按钮
│   │   ├── .cmt             ← 评论 2 …
│   │   └── …
│   └── .notebar             ← 吸底："说点什么…" + 点赞/收藏/评论
├── .mask                    ← 遮罩
└── .sheet                   ← ★ 输入弹层
    ├── .sheet__editor        ← textarea
    ├── .sheet__tools         ← 语音/图片/@/表情/＋ + 发送
    ├── .sheet__stickers      ← 表情行
    ├── .kbtools              ← 更多/剪贴板/翻译/表情/快捷 + 点击说话 + 收起
    ├── .keyboard             ← 九宫格键盘（静态）
    └── .safe                 ← 底部安全区
```

---

## 9. 移植检查清单

复刻完成后逐项对一遍：

- [ ] 输入条：白底、胶囊灰 `#F2F2F2`、高 40 圆角 20、**无边框**、图标 26px
- [ ] 输入条随列表滚动，不吸顶
- [ ] 评论 meta 里**没有竖线**
- [ ] 子回复缩进 48px，头像 26px
- [ ] @ 提及是「加粗昵称 + 上浮小号 @」
- [ ] 楼中楼默认折叠，展开有 0.24s 淡入
- [ ] **收起 = 全折**，一条不留
- [ ] 按钮文案在「展开N条回复」↔「收起回复」之间切换
- [ ] 点输入条 → 遮罩淡入 0.3s + 弹层升起 0.34s + 180ms 后聚焦
- [ ] 点遮罩 / 收起箭头 / Esc → 都能收起
- [ ] 弹层打开时输入条**被压暗而非隐藏**
- [ ] 全局缓动统一为 `cubic-bezier(.16,1,.3,1)`
- [ ] 底部笔记栏吸底，标题栏吸顶

---

*文档基于 `xhs-comment-input.html` 的实际代码整理，数值均为源码原值。*
