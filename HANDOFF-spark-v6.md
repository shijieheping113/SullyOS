# Spark 七改-UI 交接清单（2026-09-15 下午 · 公司电脑 → 家里电脑）

> 接手前先读（顺序）：`HANDOFF-spark-v5.md`（六改待办清单，仍有效）→ `WORKLOG.md` 2026-09-15 各段 → **`检讨-未等开工令擅自改码事故.md`（八条铁律，开工前必读）**。
> 本文件 = 七改-UI（评论区/输入弹层 xhs 复刻）+ v9 四轮修复的完整交接：Ann 拍板的设计决策、实现要点、验收状态。

## 分支与规矩（不变 + 新增）

- 只动 `feature/company-spark-circle-mode`，不碰其他分支，不 merge upstream
- **commit / push 前必须问 Ann**（铁律）；**没听到明确的「做吧/执行」不许动代码**——只许排查、写计划（计划用 Markdown 表格，Ann 点名的要求）；拍板选项、报反馈清单都不算开工授权
- **功能不丢原则**：原有功能只许换位置、往上加，不许删
- **看不懂就问**，别自己瞎琢磨（Ann 原话）

## 公司电脑环境档案（WORKLOG「环境备注」有全文，速记）

- shell PATH 残缺：node 工具链用绝对路径直调（`node node_modules/typescript/bin/tsc` 等），管道统计用 `node -e`
- fetch/push 要挂代理：`git -c http.proxy=http://127.0.0.1:7890 fetch origin`（一次性参数，不写进配置）
- **`.git` 松散引用文件落地即被神秘力量吞**：commit/fetch 后立刻 `git pack-refs --all --prune`；引用丢了就用文件工具直写 ref 文件再 pack（本次 commit `1e44a99b` 后又吞过一次，照此救回）
- 公司用 http 局域网预览（`http://10.48.18.221:5188`，挂 `VITE_HIDE_BUILD_BADGE=1` 关右下角 badge）；**家里才用 https 证书**（`.dev-certs/`），别搞混

## 七改-UI 已完成（全部 commit 至 `1e44a99b` + 四修待推）

Ann 验收流程：聊天窗口给反馈 → 表格计划 → 她说「做吧」→ 施工 → tsc + Spark 测试 → 手机复检。以下均已过 tsc（SocialApp 0 新错）+ Spark 94/94。

### 评论区 + 输入弹层（对照 `小红书评论输入区-设计参考.md` + `xhs-comment-input.html`）

- 评论排版：38px 头像 / 灰昵称 / 14px 正文 / meta 无竖线 / 心形赞数右侧；「共 N 条评论」吸顶节头
- 楼中楼：**默认折叠**（Ann 拍板）→「展开 N 条回复 ↔ 收起回复」横线前缀按钮，展开 0.24s 淡入；**回复楼中楼里某条评论的回复按树形紧贴被回复评论下方**，带「回复 @xx」标签，子回复缩进 48px（展开按钮 38px）
- 底部吸底互动栏：灰胶囊「说点什么…」+ 右侧 `[♡ 数字] [☆ 数字]` 水平排列（照 Ann 两张截图）
- 输入弹层：遮罩淡入 + 弹层缓动 + 180ms 延迟聚焦；收起三路（遮罩/箭头/Esc）；回复 @xx 目标胶囊可取消

### v9 四轮修复要点（改这些区域前必读）

1. **分享 = 作者纯净版**：`handleShare` 无追踪、toast「分享成功」、弹窗无追踪说明。**追踪只住两处：同步星（让角色知道弹窗）和发帖 @**。分享弹窗的「断开追踪」按钮已搬进同步弹窗底部（被追踪过的帖才显示）
2. **同步星 = 原收藏星坑位**（右上角只剩分享；原「让角色知道」入口已删）：外观一丁点不变、**无小字**（Ann 明令），点击开「让角色知道」弹窗；追踪中点亮琥珀。**没有收藏功能，只有收藏外观**（Ann 反复强调），装饰数字 = `decoCollectNum(id)`（hash%100）+ 追踪中 +1、断开回落
3. **新回复红点（一次性）**：已读水位 `initReplyWatermarks(post)`（SocialApp.tsx，localStorage 持久化 via sparkCircles.ts `load/saveSparkReplyWatermark`）。**所有打开帖子的入口必须调用它**——目前两处：`handleOpenPost` 和私聊跳帖轮询（`spark_jump_post_id` effect）。**以后新增打开入口也必须走它**，否则冒满屏红点（踩过两次坑：死代码守门条件 + 跳帖绕过）
4. **数字缩写 `fmtCount`**：≥10000→`Nw+`、≥1000→`Nk+`，整数截断；接入 3 处（详情页底栏点赞/星、列表卡片、搜索卡片），数字 span 全部 `whitespace-nowrap` 防竖排
5. **tag 单 #**：三个生成 prompt 加红线（正文禁 # 话题）+ `displayContent` 渲染时 `#xx#`→`#xx`（只洗显示不动存库）+ `parsePostTags` 剥多余 #
6. **搅动反馈**：点击 → 输入弹层立刻收起（`closeComposer()`）+ 按钮变红「正在搅动……」+ 评论区 spinner；完成后新评论淡入 + 楼层冒红点
7. **toast 唯一 id**：OSContext `addToast` 的 `Date.now()` id 加随机后缀（同毫秒连发撞 id 留鬼节点 → 白条不消失）

## 待 Ann 复检项（她说点头前不许 commit/push 后续改动）

- 红点全链路：帖子内已读消失 ✓ / 退出 Spark 重进不复发 ✓ / **私聊跳帖不冒点（本轮刚修，待测）**
- 数字缩写、同步星 +1、搅动收起弹层

## 下一个活儿：六改清单（HANDOFF-spark-v5.md，仍有效）

六改-1（emoji/图互斥）→ 2（@ 用社交 id）→ 3（帖子图进聊天卡片）→ 6（私聊多条连发）→ 5（路人认知隔离+禁抄袭）→ 4（删评论联动，**先问 Ann 意图**）。注意：本轮改动动了 SocialApp 行号，v5 里写的行号会有偏移，按函数名/关键字搜。

## 铁律新增（本轮事故沉淀，全文见检讨书）

计划模式 = 写计划（表格）→ 问「执行吗」→ 等明确开工令 → 才动代码。复述 Ann 的要求确认理解后再计划（「收藏换同步」理解错一轮，损失惨重）。改完一版等验收再改下一版。改完必写 WORKLOG。
